definition_version: 1
native_app:
  name: Protecto_Vault
  source_stage: app_src.stage
  artifacts:
    - src: app/src/*
      dest: ./
  package:
    scripts:
      - scripts/setup-package-script.sql